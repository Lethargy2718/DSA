def climbStairsDP(n, dp = {}):
    if n in dp:
        return dp[n]
    if n == 0:
        return 1
    elif n < 0:
        return 0   

    dp[n] = climbStairsDP(n - 2, dp) + climbStairsDP(n - 1, dp)
    return dp[n]

print(climbStairsDP(1000))